import React from 'react'
import { render, fireEvent, waitFor } from '@testing-library/react'

import UserCard from '../componets/UserCard'

describe("<UserCard />", () => {
    test("Should display a card",async () => {
        
    })
})